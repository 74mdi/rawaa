{
  "framework": "nextjs",
  "buildCommand": "npx prisma generate && npx prisma db push --accept-data-loss && npx prisma db seed && next build",
  "installCommand": "npm install",
  "outputDirectory": ".next"
}
